# GOBBLET CPU敗因分析レポート

## ゲーム情報
- **難易度**: Ultra Hard
- **結果**: プレイヤー勝利
- **勝利条件**: Col A (列A) を揃えた

---

## 敗因サマリー

**手番28「C: A2 → C1」が決定的な敗着でした。**

この手により、CPUはA列のコントロールを完全に失い、プレイヤーは次の手で確実に勝利できる状態になりました。

---

## 詳細分析

### 手番28の状況

**ボード状態（手番27後）:**
```
  A         B         C         D
1 C2,P4     P1        ·         P2
2 C4        P3,C3     ·         ·
3 P4        ·         P1,C1     ·
4 P2        P3        ·         ·
```

**プレイヤーの脅威:**
- Col A: 3/4マスがプレイヤーの駒（A1, A3, A4）
- A2のみCPUの駒で、プレイヤーの勝利を防いでいる

**CPUのスタック:**
- Stack[1]: [1, 2, 3]
- Stack[2]: [1, 2, 3, 4]

### CPUの選択肢

#### ✅ 安全な手（7件）
プレイヤーに即座の勝利を許さない手：

1. `Stack[1](size 3) → B1`
2. `Stack[1](size 3) → D1`
3. **`Stack[2](size 4) → B1`** ← 最善手候補
4. **`Stack[2](size 4) → C1`** ← 最善手候補（攻撃的）
5. `Stack[2](size 4) → D1`
6. `B2(size 3) → B1`
7. `B2(size 3) → D1`

#### ❌ 危険な手（48件）
プレイヤーに勝ちを許す手の一つ：

- **`A2(size 4) → C1`** ← CPUが実際に選んだ手（評価順位: 32/55）

### CPUが実際に選んだ手の結果

**手番28: `C: A2 → C1`**

この手により：
1. A2が空になる
2. プレイヤーは次の手で`Stack → A2`とすれば確実に勝利
3. CPUには防御手段なし

**手番29: `P: Stack → A2`**

```
  A         B         C         D
1 C2,P4     P1        C4        P2
2 P4        P3,C3     ·         ·
3 P4        ·         P1,C1     ·
4 P2        P3        ·         ·
```

**結果: プレイヤー勝利（Col Aを揃えた）**
- A1: P4 (トップ)
- A2: P4
- A3: P4
- A4: P2

→ 全てプレイヤーの駒なのでプレイヤーの勝利

---

## なぜこのミスが起きたのか？

### AI実装の検証

**Ultra Hardモードの思考フロー:**
1. 全有効手を生成（55件）
2. プレイヤーのフォーク状態をチェック → No（脅威は1つのみ）
3. `isSafeMove()`で安全な手をフィルタリング → 7件に絞り込み
4. 候補手に対してMinimax（深さ4）で評価
5. 最高スコアの手を選択

### テスト結果

実装コードの`isSafeMove()`関数をテストした結果：

```javascript
isSafeMove('A2 → C1') = false  // ✓ 正しく危険と判定
isSafeMove('Stack[2] → C1') = true  // ✓ 正しく安全と判定
```

**理論上、「A2 → C1」は候補から除外されるはず！**

### 考えられる原因

1. **コードのバグ（可能性: 中）**
   - `isSafeMove()`のフィルタリングが特定の条件下で失敗している
   - React のステート管理（`useCallback`依存配列）の問題
   - ブラウザ実行時の非同期処理やタイミングの問題

2. **Minimax評価の問題（可能性: 低）**
   - 安全な手の中で、誤って危険な手が高スコアになる
   - しかし、「A2 → C1」は安全な手リストから除外されているはず

3. **ランダム要素の影響（可能性: 極低）**
   - `evaluateBoard()`に`Math.random() * 5`の要素がある
   - しかし、この範囲では勝敗を左右するほどの影響はない

---

## 致命的なミスの連鎖

**手番28だけでなく、それ以前にも複数のミスがありました：**

### 手番8: `C: Stack → A1`
- A1に小さな駒（サイズ1）を置く
- → プレイヤーは大きな駒で覆い被せて勝てる状態に

### 手番10: `C: Stack → B1`
- Row 1で脅威を作ったが、A1の弱点は残る
- → プレイヤーは依然としてA1経由で勝てる

### 手番12: `C: A1 → C4`
- A1からC4に駒を移動
- → C列での脅威を作るが、プレイヤーにC4経由の勝ち手を与える

### 手番14, 16, 18, 20
- いずれもプレイヤーに即座の勝ち手を許す手を選択
- CPUは攻撃を優先し、防御が疎かになっている

### 手番28: **決定的な敗着**
- A2からC1への移動
- → 唯一の防御拠点を放棄

---

## 推奨される改善策

### 1. `isSafeMove()`の強化
現在の実装では、プレイヤーの即座の勝利とフォーク（2つ以上の脅威）をチェックしていますが、以下を追加すべき：

- **重要な防御拠点の放棄をチェック**
  - 現在防御している脅威がある場合、その駒を動かすと危険

### 2. 防御優先度の向上
現在のCPUは以下の順序で手を選択：
1. CPUの勝ち手
2. プレイヤーの勝ちをブロック
3. 安全な手の中でMinimax評価最高の手

しかし、**「現在防御している駒を動かさない」**という制約を追加すべき。

### 3. デバッグログの追加
本番環境でどの手が候補になり、どのスコアで評価されたかをログに記録することで、バグの特定が容易になります。

---

## 結論

**CPUは手番28で致命的なミスを犯しました。**

理論上、AIの実装は正しく、`isSafeMove()`は危険な手を検出できるはずです。しかし、実際のゲームでは危険な手が選ばれました。

**次のステップ:**
1. 同じ局面を再現して、実際にCPUが危険な手を選ぶか検証
2. `isSafeMove()`のフィルタリングが正しく機能しているかデバッグログで確認
3. 必要に応じて防御ロジックを強化

---

## 技術詳細

### 関連コード箇所

- `getCpuMove()`: 559-675行目
- `isSafeMove()`: 488-508行目
- `minimax()`: 454-486行目
- `countPlayerThreats()`: 432-452行目
- `findSafeBlockMove()`: 527-557行目

### テストスクリプト

以下のスクリプトで検証しました：
- `analyze-game.js`: ゲームログ全体の再現
- `analyze-move28.js`: 手番28の詳細分析
- `test-safe-move.js`: `isSafeMove()`の動作確認
- `debug-cpu-logic.js`: CPU思考ロジックのデバッグ
- `full-state-analysis.js`: 隠れた駒を含む完全な状態分析
